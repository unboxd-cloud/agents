{{- if .Values.agent.definition }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent.fullname" . }}-definition
  labels:
    {{- include "agent.labels" . | nindent 4 }}
data:
  {{ .Values.agent.name }}.agent: |
{{ .Values.agent.definition | indent 4 }}
{{- end }}
